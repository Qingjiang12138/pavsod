<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pavsod.pavsodbackend.mapper.ResultMapper">
    <select id="getTaskByUserIdAndOriginalVideoId"
            resultType="com.pavsod.pavsodbackend.entity.Task">
        select *
        from task
        where original_video_id = #{videoId} and user_id = #{userId}
    </select>

    <select id="getAiSuggest" resultType="java.lang.String">
        select ai_suggest
        from task
        where user_id = #{userId} and original_video_id = #{videoId}
    </select>
    <select id="getOriginalVideoUrlById" resultType="java.lang.String">
        select video_url
        from original_video
        where video_id = #{originalVideoId}
    </select>
    <select id="getSaliencyVideoUrlById" resultType="java.lang.String">
        select video_url
        from salient_video
        where salient_video_id = #{salientVideoId}
    </select>

</mapper>